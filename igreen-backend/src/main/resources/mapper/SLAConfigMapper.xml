<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.igreen.domain.mapper.SLAConfigMapper">

    <resultMap id="SLAConfigResultMap" type="com.igreen.domain.entity.SLAConfig">
        <id property="id" column="id"/>
        <result property="priority" column="priority"/>
        <result property="responseTimeMinutes" column="response_time_minutes"/>
        <result property="completionTimeHours" column="completion_time_hours"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, priority, response_time_minutes, completion_time_hours, created_at, updated_at
    </sql>

    <select id="selectByPriority" resultMap="SLAConfigResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sla_configs
        WHERE priority = #{priority}
    </select>

</mapper>
