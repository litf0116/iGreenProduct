<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.igreen.domain.mapper.SiteMapper">

    <resultMap id="SiteResultMap" type="com.igreen.domain.entity.Site">
        <id property="id" column="id"/>
        <result property="code" column="code"/>
        <result property="name" column="name"/>
        <result property="address" column="address"/>
        <result property="level" column="level"/>
        <result property="status" column="status"/>
        <result property="country" column="country"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, code, name, address, level, status, country, created_at, updated_at
    </sql>

    <select id="countByName" resultType="int">
        SELECT COUNT(*)
        FROM sites
        WHERE name = #{name}
    </select>

    <select id="countByCode" resultType="int">
        SELECT COUNT(*)
        FROM sites
        WHERE code = #{code}
    </select>

    <select id="countByCodeAndIdNot" resultType="int">
        SELECT COUNT(*)
        FROM sites
        WHERE code = #{code} AND id != #{id}
    </select>

    <select id="selectByNameContaining" resultMap="SiteResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sites
        WHERE name LIKE CONCAT('%', #{name}, '%')
    </select>

    <select id="selectByStatus" resultMap="SiteResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sites
        WHERE status = #{status}
    </select>

</mapper>
